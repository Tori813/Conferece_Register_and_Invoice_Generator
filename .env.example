# Copy this file to .env and fill in your real SMTP credentials
# Example: cp .env.example .env (on Windows, copy manually or use your editor)

# SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=vaokello@gmail.com
SMTP_PASSWORD=odizryolxybtsgzf
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=vaokello@gmail.com
SMTP_FROM_NAME="ConferenceApp"

# Optional limits (bytes). Default applied if not set.
MAX_UPLOAD_BYTES=5242880
